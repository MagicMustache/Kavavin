<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/cellarFragment">
    <fragment
        android:id="@+id/cellarFragment"
        android:name="ch.milog.kavavin.fragments.CellarFragment"
        android:label="Ma cave"
        tools:layout="@layout/fragment_cellar">
        <action
            android:id="@+id/action_cellarFragment_to_addFragment3"
            app:destination="@id/addFragment3" />
    </fragment>
    <fragment
        android:id="@+id/addFragment3"
        android:name="ch.milog.kavavin.fragments.AddFragment"
        android:label="Ajouter une bouteille"
        tools:layout="@layout/fragment_add" />
</navigation>